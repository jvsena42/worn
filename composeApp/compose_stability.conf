// Types defined in :shared, which is compiled without the Compose compiler plugin and therefore
// carries no @StabilityInferred metadata. Every class below is an immutable data class whose
// collection fields are never mutated in place — state always moves forward through copy() — so
// it satisfies Compose's stability contract and composables taking them can skip.
com.github.worn.domain.model.*
com.github.worn.presentation.viewmodel.*
