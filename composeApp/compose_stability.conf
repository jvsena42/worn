// :shared has no Compose compiler plugin, so these carry no stability metadata. They are
// immutable data classes, only ever replaced via copy(), so declaring them stable is sound.
com.github.worn.domain.model.*
com.github.worn.presentation.viewmodel.*
