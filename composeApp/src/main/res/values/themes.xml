<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Only the pre-Compose window matters here: everything after first frame is drawn by
      WornTheme. windowBackground is what shows between the splash handing off and Compose's
      first frame, so it tracks the app's page colour via values-night rather than being pinned.

      Deliberately no statusBarColor/navigationBarColor: enableEdgeToEdge() in MainActivity owns
      system bar appearance, and setting them here fights it (they were pinned to the splash
      green, which is why content ran under an opaque green status bar).
    -->
    <style name="Theme.Worn" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>

    <style name="Theme.Worn.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_green</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/ic_launcher_background</item>
        <item name="postSplashScreenTheme">@style/Theme.Worn</item>
    </style>
</resources>
